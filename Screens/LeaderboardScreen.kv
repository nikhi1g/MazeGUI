<LeaderboardScreen>:
    #from kivy.clock import Clock
    on_enter: root.enter()
    #on_enter: Clock.schedule_once(self.enter, 3)

    leaderboard: leaderboard

    name: 'leader'
    FloatLayout:
        size_hint: None, None

    Label:
        id: leaderboard
        text: 'To StartScreen'
        size: 100, 100
        pos_hint:{"x":0,"y": 0}
        color: 0.3, 0.3, 0.3, 1
